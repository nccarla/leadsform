# Copia este archivo a .env (no subir .env al repositorio).
# API Node lee DATABASE_URL al arrancar (puerto API: 3001 por defecto).
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/formulario_leads
PORT=3001
VITE_API_BASE_URL=http://localhost:3001
